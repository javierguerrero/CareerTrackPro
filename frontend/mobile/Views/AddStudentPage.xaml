<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MusicSchool.Mobile.Views.AddStudentPage"
             Title="Nuevo alumno">
    <VerticalStackLayout  Padding="20" Spacing="20">

        <Label Text="Ingresar datos:" />
        
        <Entry Placeholder="Nombre" Text="{Binding Name}" />
        <Entry Placeholder="Apellido" Text="{Binding Name}" />
        <Entry Placeholder="Teléfono" Text="{Binding Name}" />
        <Entry Placeholder="Correo" Text="{Binding Name}" />
        <Editor Placeholder="Notas" HeightRequest="150" Text="{Binding Name}"  />
        
        <Button Text="Guardar" Command="{Binding SaveTapCommand}"  />

    </VerticalStackLayout>
</ContentPage>